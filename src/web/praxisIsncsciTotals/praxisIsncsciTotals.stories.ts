import {html} from 'lit';
import type {Meta, StoryObj} from '@storybook/web-components';
import './praxisIsncsciTotals';

const meta = {
  title: 'WebComponents/PraxisIsncsciTotals',
  // tags: ['autodocs'],
  render: () => html`<praxis-isncsci-totals></praxis-isncsci-totals>`,
} satisfies Meta;

export default meta;
type Story = StoryObj;

export const Primary: Story = {};
