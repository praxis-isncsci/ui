{/* ColorGradient.mdx */}

import {Canvas, Meta} from '@storybook/blocks';
import * as ColorGradientStories from './colorGradient.stories';

<Meta of={ColorGradientStories} />

# Color

## Design Problem

We need a component in our color stories that shows all color swatches as a gradient so that we can easily tell the differences in luminicense.

## Solution

A component that shows all color swatches as a gradient.

---

## Primary

<Canvas of={ColorGradientStories.Primary} />
